# C4 Model

## Level 1 — System Context

```mermaid
flowchart LR
    User[Operator / User]
    RedPA[RedPA AI Platform]
    Ollama[LLM Providers]
    External[External Systems]
    User --> RedPA
    RedPA --> Ollama
    RedPA --> External
```

RedPA AI is an enterprise-oriented agentic AI platform for orchestrating
agents, tools, workflows, models, policy enforcement, retrieval, and human
oversight.

## Level 2 — Containers

```mermaid
flowchart TB
    Browser[Web Control Center]
    API[FastAPI Backend]
    Policy[Spring Boot Policy Service]
    Worker[Background Runtime]
    Postgres[(PostgreSQL)]
    Qdrant[(Qdrant)]
    Redis[(Redis)]
    MCP[MCP Services]
    Ollama[Ollama / Model Providers]
    Prom[Prometheus]
    Grafana[Grafana]
    OTel[OpenTelemetry Collector]
    Tempo[Tempo]

    Browser --> API
    API --> Policy
    API --> Postgres
    API --> Qdrant
    API --> Redis
    API --> MCP
    API --> Ollama
    Worker --> Postgres
    Worker --> Redis
    API --> Prom
    API --> OTel
    OTel --> Tempo
    Prom --> Grafana
    Tempo --> Grafana
```

## Level 3 — FastAPI Components

```mermaid
flowchart TB
    Router[API Routers]
    AppServices[Application Services]
    Agents[Agent Runtime]
    Gateway[Model Gateway]
    Guardrails[Guardrails / Policy Client]
    Tools[Tool & MCP Runtime]
    Reviews[Human Review]
    Memory[Memory / Retrieval]
    Persistence[Repositories / SQLAlchemy]

    Router --> AppServices
    AppServices --> Agents
    AppServices --> Gateway
    AppServices --> Guardrails
    AppServices --> Tools
    AppServices --> Reviews
    AppServices --> Memory
    AppServices --> Persistence
```

## Level 3 — Policy Service

```mermaid
flowchart LR
    Controller[REST Controller]
    Engine[Policy Engine]
    Rules[Policy Rules]
    Domain[Policy Domain]
    Controller --> Engine
    Engine --> Rules
    Rules --> Domain
```
