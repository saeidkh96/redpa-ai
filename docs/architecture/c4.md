# RedPA AI V6.0 — C4 Model

This document describes the architecture represented by the V6.0 repository. It distinguishes implemented local runtime components from deployment/reference assets.

## Level 1 — System Context

```mermaid
flowchart LR
    Developer[Developer]
    Operator[Operator]
    User[API Consumer]
    RedPA[RedPA AI Platform]
    Models[LLM Providers]
    External[External Systems / GitHub / Tools]

    Developer -->|Python SDK / CLI| RedPA
    Operator -->|Next.js Control Plane| RedPA
    User -->|HTTP API| RedPA
    RedPA --> Models
    RedPA --> External
```

RedPA AI is a developer-facing Agentic AI platform for orchestrating agents, tools, durable workflows, retrieval, model access, policy enforcement, human oversight, evaluation, memory, and operational telemetry.

## Level 2 — Runtime Containers

```mermaid
flowchart TB
    SDK[Python SDK / redpa CLI]
    Browser[Next.js Control Plane :3001]
    API[FastAPI Backend :8000]
    Policy[Spring Boot Policy Service :8090]

    Worker[Background Worker]
    Scheduler[Background Scheduler]
    Outbox[Outbox Publisher]

    Coordinator[A2A Coordinator :8050]
    Research[Research Agent :8061]
    PGAgent[PostgreSQL Agent :8062]
    DockerAgent[Docker Agent :8063]
    FSAgent[Filesystem Agent :8064]
    GHAgent[GitHub Agent :8065]

    FSMCP[Filesystem MCP :8010]
    GHMCP[GitHub MCP :8020]
    PGMCP[PostgreSQL MCP :8030]
    DockerMCP[Docker MCP :8040]

    Postgres[(PostgreSQL :5432)]
    Qdrant[(Qdrant :6333/6334)]
    Redis[(Redis :6379)]

    OTel[OpenTelemetry Collector :4317/4318]
    Tempo[Tempo :3200]
    Prom[Prometheus :9090]
    Grafana[Grafana :3000]
    Models[Model Providers]

    SDK --> API
    Browser --> API
    API --> Policy
    API --> Coordinator
    API --> FSMCP
    API --> GHMCP
    API --> PGMCP
    API --> DockerMCP
    API --> Postgres
    API --> Qdrant
    API --> Redis
    API --> Models

    Coordinator --> Research
    Coordinator --> PGAgent
    Coordinator --> DockerAgent
    Coordinator --> FSAgent
    Coordinator --> GHAgent

    Worker --> Postgres
    Worker --> Redis
    Scheduler --> Postgres
    Scheduler --> Redis
    Outbox --> Postgres
    Outbox --> Redis

    API --> OTel
    API --> Prom
    OTel --> Tempo
    Prom --> Grafana
    Tempo --> Grafana
```

The policy service is supplied by `docker-compose.phase13.yml`, which augments the main Docker Compose stack and configures the backend to use it.

## Level 3 — FastAPI Platform Components

```mermaid
flowchart TB
    Router[API Routers]
    Identity[Auth / Tenancy / OAuth]
    Agents[Agent Runtime / Registry]
    Durable[Distributed Durable Workflows]
    Reviews[Human Review]
    Memory[Agent Memory]
    RAG[RAG / Retrieval]
    Gateway[Model Gateway]
    Evaluation[Evaluation / Benchmark / Release Gates]
    Tools[Unified Tools / MCP]
    A2A[A2A / Remote & Specialist Agents]
    Events[Transactional Outbox / Events]
    Governance[Guardrails / Policy Enforcement]
    Persistence[Models / Repositories / SQLAlchemy]
    Runtime[Background Jobs / Runtime Cache]
    Observability[Metrics / Logs / Traces / Performance]

    Router --> Identity
    Router --> Agents
    Router --> Durable
    Router --> Reviews
    Router --> Memory
    Router --> Gateway
    Router --> Evaluation
    Router --> Tools
    Router --> A2A
    Router --> Events
    Router --> Governance

    Agents --> RAG
    Agents --> Gateway
    Agents --> Durable
    Durable --> Reviews
    Tools --> Governance
    Tools --> A2A

    Identity --> Persistence
    Durable --> Persistence
    Reviews --> Persistence
    Memory --> Persistence
    Evaluation --> Persistence
    Events --> Persistence
    Runtime --> Persistence

    RAG --> Qdrant[(Qdrant)]
    Memory --> Qdrant
    Runtime --> Redis[(Redis)]
    Events --> Redis
    Observability --> Prometheus[Prometheus / OTel]
```

## Level 3 — Policy Service

```mermaid
flowchart LR
    Controller[REST Controller]
    Engine[Policy Engine]
    Rules[Policy Rules]
    Decision[ALLOW / REVIEW / DENY]
    Audit[Policy Audit]

    Controller --> Engine
    Engine --> Rules
    Rules --> Decision
    Decision --> Audit
```

## Developer Boundary

The V6 Python SDK and CLI are clients of implemented HTTP APIs. They do not duplicate server-side orchestration, governance, durable state, or policy logic.

## Deployment Boundary

Docker Compose is the validated local integration runtime. Kubernetes/Helm and Azure/Pulumi are deployment/reference assets in the repository; their presence is not evidence of a live hosted production deployment.
